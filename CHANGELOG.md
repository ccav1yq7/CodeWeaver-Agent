# 1.0.0a1 — 2026-09-07

New implementation: capability authorization, protected file operations, mandatory command isolation,
bounded provider loops, durable receipts, session resume, complete-group context reduction, durable notes,
explicit MCP/skills, detached workers and hidden grading. All interfaces use one shared runtime.

Legacy source, tests, assets, credentials and traces are excluded. No legacy benchmark claims are inherited.
See `docs/MIGRATION.md` for intentional compatibility changes and `docs/VERIFICATION.md` for performed checks.
